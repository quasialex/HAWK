<a class="back" href="/logs">← Logs</a>
<h2>Log: <%= @file %></h2>

<% text = (@content || '').to_s %>
<% if text.empty? %>
  <p class="muted">No content yet…</p>
<% else %>
  <pre class="log"><%= text %></pre>
<% end %>
